"use client";

import { CatalogScreen } from "@/components/catalog/CatalogScreen";

/**
 * Product Catalog route — free browse (no slot/kind locks).
 */
export function CatalogPage() {
  return <CatalogScreen />;
}
