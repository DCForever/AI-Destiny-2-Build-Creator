export const metadata = { title: "Debug — Catalog" };

export default function DebugCatalogPage() {
  return (
    <p className="text-sm text-zinc-400">
      Catalog debug UI — implemented in Phase 4 (US2).
    </p>
  );
}
