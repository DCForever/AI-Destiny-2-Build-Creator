export const metadata = { title: "Debug — Builds" };

export default function DebugBuildsPage() {
  return (
    <p className="text-sm text-zinc-400">
      Build debug UI — implemented in Phase 5 (US3). Use REST APIs under <code>/api/user/builds</code> until then.
    </p>
  );
}
