/** Stub roll-alternative matcher (T018). */
export function findRollAlternatives(
  ..._args: [number, number[]]
): Array<{ itemHash: number; name: string; matchingPerks: number }> {
  void _args;
  return [];
}
