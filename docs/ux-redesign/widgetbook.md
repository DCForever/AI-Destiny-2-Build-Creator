# Widgetbook (Catalog)

Isolated design catalog for Flutter Matte Flap / Neon composables.

| | |
| --- | --- |
| App | `flutter/apps/widgetbook` (`destiny2_widgetbook`) |
| Widgets | `flutter/packages/ui_flutter` |
| Run | See [`flutter/apps/widgetbook/README.md`](../../flutter/apps/widgetbook/README.md) |

## Why

Area-implement dual-truth shots prove host parity. Widgetbook is for **component isolation** (states, signed-out honesty, multi-hash family chips) without OAuth or live inventory.

## Phase 1 (shipped)

Catalog folders under Widgetbook:

- Meta, Cards, Family (incl. multi-hash Base → one chip), Facets, Scope, Empty
- Group (header + outline rail)
- Detail (instance strip, owned/unowned detail, perk grid, toggles)
- Workspace (grid + 400 detail / grid only)

## Phase 2 (backlog)

From catalog implement-report `widgetbook_backlog` entries not yet storied:

- `CatalogSortGroupSheet` reorder keys + dims
- Primary-line type-icon filter bar composition
- Outline jump expand-on-jump demo with scroll
- Mobile structure-only detail push viewport
- Knobs for element / slot / ownedCount
- Neon board primitives / atmosphere

## Rules

- No Widgetbook dependency on pure packages or `ui_flutter`
- Fixtures pure Dart only (no secrets, no network requirement for layout)
- Does **not** replace Capture dual-truth gates
