# area-ux-redesign report

Area: catalog / browse-chrome

## Mockups
{"desktop_path":"docs/ux-redesign/catalog/mockups/001-browse-chrome-desktop.html","flows_demonstrated":["family-card-one + owned-version-chips-readonly + ×N sum (GAP-001)","detail-all-versions + full rebind switch + family selection sticky (GAP-001)","group-collapse headers + outline jump rail/strip scroll-only (GAP-002)","sort-priority-reorder + group-priority-reorder progressive sheet (GAP-003)","type-filter-icons primary-line silhouettes + a11y title (GAP-004)","facet off/include/exclude cycle + search + BR-CAT-006/007 match semantics","signed-out no owned chips; owned-empty Sync; zero-match Clear; loading skeleton; error Retry","disabled Set/Synergy stubs with honest deferred copy","mobile structure parity: collapse + outline strip + bottom sheet reorder (push deferred)"],"mobile_path":"docs/ux-redesign/catalog/mockups/001-browse-chrome-mobile.html","notes":"Approved via MOCKUP-APPROVED.md (continue with workflow). Closes mockup SSoT for GAP-CAT-BROWSE-001–004. Density locks 200×112 cards + 400px detail. Version chips pointer-events:none (non-selectable). Outline hidden when flat or <2 groups. Mobile is structure parity only—dual-truth gate remains windows. GAP-CAT-PERK-003 craft ON left non-blocking."}

## Brief
{"acceptance":["One family card per name-normalized + slot/element/type family (not one definition hash)","Owned-only non-selectable version chips on grid; omit unowned; signed-out never fakes owned","Card art/name base-primary; tap uses openVersion (matching when disambiguated else primary)","Detail lists all family versions + full identity rebind switch; family grid selection sticky","Owned ×N sums family instances; family shows if any member matches; exclude only if all excluded","Collapsible group sections default expanded; collapse does not filter (BR-CAT-007)","Outline jump rail only when group-by + ≥2 groups; scroll/expand only; never filters","Sort & group progressive sheet: reorderable sort keys + active group dims; persist; default slot→exotic→ammo→archetype→name","Primary-line weapon-type silhouette filters + Semantics/tooltip; no text archetype under More","BR-CAT-006 filter semantics unchanged; density 200×112 cards + 400 detail; Neon/Flap only","Set/Synergy stubs disabled; no Catalog-as-home; residual perk rules held","Widget tests + windows host smoke green; shot matrix proves family/group/sort/type tokens","Mobile structure parity only — not dual-truth exit gate","GAP-CAT-PERK-003 remains open non-blocking residual"],"locked_decisions":["Single dual-truth exit for GAP-CAT-BROWSE-001–004; implement order 001→004; windows gate; mobile structure-only","Family merge: Adept/Holofoil-class via name-normalized + slot/element/type only; no invented collectible graph; under-merge OK","One grid card per family; base art+cleaned name when present else first member; openVersion = matching member when filters disambiguate else primary","Grid version chips: owned-only non-selectable static micro-indicators; omit unowned; signed-out never fakes owned; not NeonFacetChip cycle","Owned ×N = sum across family members; family visible if any member matches filters (exclude drops only if all excluded)","Detail: all family versions + switch; full hash rebind (perks/pools/catalyst/instances); unowned listable for inspect; family card selection sticky","Groups: all expanded default; header label+count+chevron toggles collapse only (no filter rewrite)","Outline rail only when group-by active and ≥2 groups; sticky; jump/scroll + expand; never filters (BR-CAT-007)","Progressive Sort & group sheet: reorderable sort keys (default slot→exotic→ammo→archetype→name) + active group dims; persist last; no permanent dual lists","Weapon-type filters: primary-line iconOnly kWeaponTypeOfficial + Semantics/tooltip; letter last-resort; drop text archetype from More","Density lock maxCrossAxisExtent 200 / mainAxisExtent 112 / kCatalogWeaponsDetailWidth 400","Composition aid only (DBR-PUR-002); no Catalog-as-home; Set/Synergy disabled stubs only","Residual-polish perk chrome held; GAP-CAT-PERK-003 non-blocking this exit","Neon/Flap + official Destiny icons only — no greenfield DS"],"mockup_paths":["docs/ux-redesign/catalog/mockups/001-browse-chrome-desktop.html","docs/ux-redesign/catalog/mockups/001-browse-chrome-mobile.html","docs/ux-redesign/catalog/MOCKUP-APPROVED.md","docs/ux-redesign/catalog/implementation-shots/001-residual-polish/COMPARE.md"],"obsidian_path":"requirements/Projects/Destiny 2 Build Creator/UX/UX Catalog — Browse chrome.md","out_of_scope":"Live Set/Synergy CTAs or Catalog-local create/edit; armor family parity as dual-truth gate; inventing collectible/pattern graph beyond name-normalized + slot/element/type guard; selectable version chips on grid; Catalog-as-home / nav or default-route IA; greenfield design system or board density redesign (keep 200×112 cards / 400 detail); mobile Catalog push as exit gate; GAP-CAT-PERK-003 craft ON dual-truth as blocker; vault transfer/notes/lock; changing BR-CAT-006 include OR / across AND / exclude drop.","rule_ids":["DBR-PUR-002","DBR-ROLL-010","DBR-UI-001","DBR-UI-005","DBR-UI-006","DBR-UI-007","DAC-NME-003","BR-CAT-001","BR-CAT-002","BR-CAT-004","BR-CAT-006","BR-CAT-006a","BR-CAT-006b","BR-CAT-007","BR-CAT-010c","BR-CAT-016b","GAP-CAT-BROWSE-001","GAP-CAT-BROWSE-002","GAP-CAT-BROWSE-003","GAP-CAT-BROWSE-004","GAP-CAT-PERK-003"],"slice_goal":"Family weapon cards (one card per regular/adept/holofoil family): owned-only non-selectable version indicator chips on grid; card opens primary/matching version; detail shows all family versions with full-rebind switch. Collapsible group-by sections + outline jump rail. User-reorderable sort priority and group-by dimension priority. Weapon type filters as official type silhouette icons. Dual-truth exit closes GAP-CAT-BROWSE-001–004 together on windows.","surfaces":["catalog.signed-out.weapons","catalog.weapons.owned","catalog.weapons.manifest","catalog.weapons.filters","catalog.weapons.filter.element","catalog.weapons.filter.ammo","catalog.weapons.filter.archetype","catalog.weapons.filter.slot","catalog.weapons.filter.groupby","catalog.weapons.list","catalog.weapons.instance-card","catalog.weapon.detail","catalog.composition-aid","flow.catalog.weapons"],"title":"Catalog / browse-chrome — family cards, groups, sort/group priority, type silhouettes"}

## Architect
{"anti_bloat":["No new package; no invent collectible/pattern graph beyond name-normalized + slot/element/type","No selectable grid version chips; do not reuse NeonFacetChip cycle for versions","No Catalog-as-home / default-route IA; Set/Synergy stay disabled stubs only","No greenfield design system or density redesign (keep 200×112 / 400)","Do not change BR-CAT-006 include OR / across AND / exclude drop","Do not put family-merge, sort keys, or filter predicates only in widgets or host","Do not grow domain/sandbox_data with Flutter, IO, or catalog UI","Do not dual-gate mobile Catalog push or block on GAP-CAT-PERK-003 craft ON"],"package_placement":"No new package. Pure weapon-family merge (name-normalized + slot/element/type key, primary/openVersion, owned ×N sum, any-member filter survival), multi-key sort comparator (default slot→exotic→ammo→archetype→name), and ordered group-by remain in packages/manifest — BR-CAT-006 filter semantics unchanged; collapse/outline never rewrite filters (BR-CAT-007). Optional pure prefs DTO/encode in packages/app only; IO persist stays host-side. Neon/Flap composition (family card + owned-only non-selectable version chips, collapsible group headers, outline jump rail when ≥2 groups, progressive Sort & group reorder sheet, primary-line iconOnly type silhouettes via kWeaponTypeOfficial + Semantics/tooltip, detail family version switch with full hash rebind, existing 200×112 grid + 400 detail workspace/empty/skeleton) extends packages/ui_flutter/catalog only. Thin orchestration, sticky family selection, openVersion on tap, rebind load, and last sort/group persist live in apps/windows_host/lib/catalog; mobile is structure parity only. Keep domain/sandbox_data pure (no Flutter/IO, no invent collectible graph); hosts never own family-merge predicates.","risks":["Name-normalization over-merge across distinct weapons; slot/element/type guard + under-merge-OK must be golden-tested","Detail needs all family members from full base set, not only currently filtered survivors — wrong source drops unowned versions","openVersion vs sticky family id vs selected hash rebind can desync selection highlight if keys mix familyId and hash","God CatalogPage growth: residual polish + family/group/sort in one state object risks regressions without pure + widget coverage","Outline rail + CustomScrollView scroll-to-group + expand-on-jump is fragile without GlobalKeys/Scrollable.ensureVisible","Primary-line type silhouettes (many weapon types) may overflow; must scroll/wrap without burying under More","Full-manifest family merge on every chip refilter may jank — keep pure/O(n) and avoid widget-layer rebuild thrash","Persisting sort/group last-order without pure encode tests or host-only prefs may silently reset defaults","Treating version chips as selectable or facet-cycleable breaks GAP-001 dual-truth","Mobile structure work accidentally pulled into dual-truth exit gate (windows remains gate)"],"widget_inventory":["CatalogWeaponFamilyCard (one family; base art+cleaned name; owned ×N sum; selection sticky)","CatalogOwnedVersionChips (owned-only static micro-indicators; non-selectable; omit unowned; signed-out never fakes)","CatalogWeaponsGrid (family cards; density 200×112; optional collapsible groups)","CatalogGroupSectionHeader (label+count+chevron; default expanded; collapse view-only)","CatalogGroupOutlineRail (sticky; only group-by + ≥2 groups; jump/scroll+expand; never filters)","CatalogSortGroupSheet (reorderable sort keys + active group dims; progressive; persist last)","CatalogFilterBar (primary-line type silhouettes + element/slot; drop text archetype from More)","NeonFacetChip (iconOnly weapon-type visual + Semantics/tooltip; off/include/exclude cycle)","CatalogFamilyVersionSwitch (detail: all family versions; owned marks; full identity rebind)","CatalogWeaponDetail (rebind perks/pools/catalyst/instances on version switch; residual perk chrome held)","CatalogWeaponsWorkspace (main + fixed 400 detail; density lock)","CatalogEmptyState (zero-match Clear; owned-empty Sync; signed-out Owned honesty)","CatalogLoadingSkeleton / error Retry","CatalogOutboundStubs (Set/Synergy disabled + deferred copy)","CatalogScopeControl (All/Owned; signed-out never fakes owned)","CatalogPage thin host wire (windows gate; mobile structure-only)"],"widget_test_inventory":["CatalogWeaponFamilyCard: one card per family not per hash; base name/art when present","CatalogOwnedVersionChips: owned-only chips; omit unowned; signed-out never fakes owned/×N","CatalogOwnedVersionChips: non-interactive (no selection/onTap; not NeonFacetChip cycle)","CatalogWeaponsGrid: family ×N sums member ownedCounts; tap invokes openVersion not chip","openVersion/family pure+UI: matching when filters disambiguate else primary","CatalogGroupSectionHeader: default expanded; collapse hides section without filter rewrite","CatalogGroupOutlineRail: hidden when flat or <2 groups; visible when group-by + ≥2","CatalogGroupOutlineRail: jump expands collapsed target and scrolls only (no filter change)","CatalogSortGroupSheet: reorder sort keys changes result order; default slot→exotic→ammo→archetype→name","CatalogSortGroupSheet: reorder active group dims changes nesting; inactive dims not applied","NeonFacetChip type iconOnly: official silhouette + Semantics/tooltip; letter last-resort","CatalogFilterBar: weapon-type primary-line; no text archetype under More for weapons","CatalogFamilyVersionSwitch: lists all family versions; unowned listable; switch rebinds selected hash","Family selection sticky across version rebind and refilter when family still visible","NeonFacetChip / filters: BR-CAT-006 off→include→exclude; exclude drops only all-member families","CatalogEmptyState + workspace density: zero-match Clear; 200×112 cards; 400 detail pane"]}

## Verdicts
[{"agent_id":"019fd2b2-ac82-7922-bdaa-0ed43d5cbdd5","cancelled":false,"duration_ms":151943,"output":{"issues":["GAP-001 openVersion dual-SSoT: MOCKUP-APPROVED requires owned max-power else base; mock JS + locked acceptance use base-primary + filter disambiguation only (e.g. Beloved opens unowned Base while only Adept is owned).","Invented rule IDs BR-CAT-010c / BR-CAT-006a / BR-CAT-006b cited in mock/brief rule_ids but absent from specs/business-rules.md (only BR-CAT-001–009 and BR-CAT-016*).","Residual perk hard chrome not held in browse mock SSoT (BR-CAT-016/016a): no ①/②/③ cell badges, no ② gold chevron, no enhanced gold/E — residual dual-truth closed patterns dropped.","BR-CAT-016b residual meta strip incomplete on browse detail: frame chip missing (type/slot/element/ammo/×N only; frame as text subtitle).","Claimed vault path UX Catalog — Browse chrome.md does not exist; browse family/openVersion rules lack Obsidian SSoT (only UX Catalog — Weapons.md present).","DUAL-TRUTH-GAPS GAP-CAT-BROWSE-001–004 still list Mockup SSoT as (pending) after MOCKUP-APPROVED + 001-browse-chrome-*.html exist."],"ok":false},"success":true,"tokens_used":418204},{"agent_id":"019fd2b2-ac82-7922-bdaa-0ee0537f04b1","cancelled":false,"duration_ms":78806,"output":{"issues":["Primary filter chrome packs ~20+ controls; flex-wrap can stack 2–3 rows on narrow desktop (mitigated by More + progressive sort/outline).","Detail PL instance chips are buttons with aria-pressed but no handlers and no disabled/deferred honesty — false instance-select affordance vs version rebind.","empty-sync Sync/Settings and error Settings CTAs lack handlers and lack disabled+deferred copy (unlike Set/Synergy stubs).","Card open defaults to primary/base among filter matches, not owned max-power per MOCKUP-APPROVED — residual product-rule drift.","Mobile omits instance strip, mode switch, and secondary facets — structure parity only; detail instance story is desktop-only (banner acknowledges dual-truth=windows).","Owned version chips on cards show labels only; tooltips lack per-version counts (aggregate ×N only) — mild identity clarity residual."],"ok":true},"success":true,"tokens_used":234069},{"agent_id":"019fd2b2-ac82-7922-bdaa-0ef27fdc68b2","cancelled":false,"duration_ms":111922,"output":{"issues":["God-page already real: windows CatalogPage ~1303 lines owns load/facets/selection/rebind/group/empty/armor outbound; family+sort+outline must not grow same State object — extract controller/selectors or pure pipeline first.","Family merge/openVersion absent from packages/manifest; only per-hash CatalogItem + normalizeName — must golden-test name-norm+slot/element/type under-merge-OK before UI.","filterCatalogClient is per-hash BR-CAT-006; family any-member survival / exclude-all-members drop does not exist — pure layer only, never widgets/host predicates.","Detail all-versions must read full annotated base, not filtered _results; current selection clears when hash leaves survivors — wrong source risk.","Sticky familyId vs openVersion hash vs selectedHash highlight desync if keys mixed; needs pure openVersion + host sticky id separate from hash rebind.","Multi-key reorderable sort missing (compareCatalogWeapons fixed only); group collapse/outline/SortGroupSheet widgets missing — pure comparator+ordered group dims in manifest, UI in ui_flutter/catalog only.","Widget inventory aspirational: no CatalogWeaponFamilyCard/OwnedVersionChips/GroupSectionHeader/OutlineRail/SortGroupSheet/FamilyVersionSwitch; grid still hash cards 200×112.","Widget-test inventory mostly missing for family/group/sort/openVersion/sticky/outline-no-filter-rewrite; only facet cycle, hash grid, empty/workspace/detail residual exist.","Outline rail + CustomScrollView ensureVisible+expand is unproven fragility; prefs sort/group encode absent in app — pure DTO tests + host-only IO required to avoid silent default reset.","Full-manifest family merge on every chip refilter can jank if not pure O(n) and host rebuilds thrash; mobile is deferred — keep structure-only, do not dual-gate windows dual-truth.","Do not put family-merge/sort/filter in host or widgets; do not grow domain/sandbox_data; do not treat version chips as NeonFacetChip-selectable (GAP-001 dual-truth)."],"ok":true},"success":true,"tokens_used":319818}]

Next: run area-implement with brief_path pointing at the locked brief.
