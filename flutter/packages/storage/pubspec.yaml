name: destiny2_storage
description: >
  StorageRoot abstraction for Destiny 2 Build Creator multiplatform port.
  App-support layout paths (not repo .cache). Hosts inject path_provider
  application-support paths on Flutter Windows (DART-012).
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
