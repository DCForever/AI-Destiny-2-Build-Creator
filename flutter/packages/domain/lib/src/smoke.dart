/// Trivial pure symbol used by the DART-001 monorepo smoke test.
///
/// Later slices replace/extend this package with real domain types and
/// evaluators; keep this function pure (no IO).
String domainPackageSmokeLabel() => 'destiny2_domain';
