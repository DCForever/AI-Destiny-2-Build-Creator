name: destiny2_ui_tokens
description: >
  Shared Matte Flap Ledger design tokens + FlapBoard layout contracts (DART-029).
  Pure Dart — no Flutter/Jaspr runtime deps. Hosts map ARGB ints to Color/CSS.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

# Runtime dependencies: Dart SDK only.
# Do NOT add Flutter, Jaspr, Drift, http, path_provider, or other IO/UI packages.
dependencies: {}

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
